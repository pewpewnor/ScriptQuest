import { FC } from "react";

interface NavbarProps {}

const Navbar: FC<NavbarProps> = (props: NavbarProps) => {
	return <div className="absolute top-0 bg-lime h-12 w-full"></div>;
};

export default Navbar;

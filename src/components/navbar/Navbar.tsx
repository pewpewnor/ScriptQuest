import { FC } from "react";

interface NavbarProps {}

const Navbar: FC<NavbarProps> = (props: NavbarProps) => {
	return <div className="absolute top-0 h-12 w-full bg-lime"></div>;
};

export default Navbar;
